# Copy to .env and fill in your values

# Workspace directory to mount (default: ~/Workspace)
WORKSPACE=/home/gavinyap/Workspace

# Git identity (optional — falls back to mounted .gitconfig)
GIT_USER_NAME=Gavin Yap
GIT_USER_EMAIL=

# API key (optional — only needed if using SDK, not needed for Max subscription)
ANTHROPIC_API_KEY=
